name: vibe-guard security scan

on: [push, pull_request]

jobs:
  vibe-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Vibe-Guard Action
        uses: ./
        with:
          path: vibe_guard
          fail-on: critical

